<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.screentime">

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <!-- 1. REQUIRED PERMISSIONS FOR USAGE MONITORING (app_usage package) -->
    <!-- This permission requires user to manually grant access in settings -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions"/>

    <!-- 2. REQUIRED PERMISSIONS FOR BACKGROUND TASKS (workmanager package) -->
    <!-- Used by Workmanager for scheduling tasks -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/> 
    
    <!-- 3. REQUIRED FOR NOTIFICATIONS (Android 13+ devices) -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="screentime"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- WORKMANAGER: Ensure the WorkManager components are registered (often done by the plugin itself, but good practice to verify or include if the plugin is older) -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>